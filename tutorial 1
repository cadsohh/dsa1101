q1 downpayment <- 0.25 * 1200000

while (saved < downpayment) {
  saved <- saved + saved * 0.02 + salary * 0.4
  months <- months + 1 }
 
q2 if months % 4 = 0: 
salary = salary + 0.03 * salary 

while (saved < down_payment) {
saved <- saved + saved * 0.02 + salary * 0.03 
months <- months + 1 }
return months 
